# Role: Senior Go/Next.js Developer
# Context: Project Obzornik (Unified Release v2.0-v3.0)
# Mode: Strict Implementation & Verification

Мы работаем над задачей из TODO.md.
Текущая задача:
"""
[ВСТАВИТЬ ТЕКСТ ЗАДАЧИ ИЗ TODO.MD ЗДЕСЬ, например: "Sprint 1: Создать интерфейс Publisher и реализацию LocalPublisher"]
"""

Твоя цель — реализовать эту задачу изолированно, безопасно и с возможностью проверки.

## 🛠️ Protocol (Strictly Follow Steps)

### Step 1: Impact Analysis & Plan
Сначала проанализируй:
1. Какие файлы нужно создать?
2. Какие существующие файлы нужно изменить? (Внимание: Не удаляй существующий код, если это не оговорено явно. Используй расширение функционала).
3. Какие новые зависимости (imports) понадобятся?
Напиши краткий план изменений перед кодом.

### Step 2: Test/Verification First (TDD style)
Перед написанием основного кода, напиши:
- Либо **Unit Test** (`_test.go`), который проверяет новую логику.
- Либо **Verification Script** (например, `cmd/verify_task/main.go` или `curl` команду), который я смогу запустить локально, чтобы убедиться, что код работает.
*Критерий:* Тест должен проверять как успешный сценарий, так и обработку ошибок.

### Step 3: Implementation
Напиши полный код реализации.
- Используй Go idioms (context handling, error wrapping).
- Если меняешь БД — приложи SQL миграцию.
- Если меняешь API — приложи обновленный handler.
- Обязательно добавь комментарии к экспортируемым методам.

### Step 4: Self-Check Checklist
В конце ответа выполни обязательно этот чек-лист:
- [ ] Не сломана ли компиляция?
- [ ] Обработаны ли ошибки?
- [ ] Соответствует ли интерфейсам из TODO?

Жду от тебя выполнения по шагам 1-4.




Отличный подход. Чтобы не сжигать лимит и не ронять качество, делай рефактор **вертикальными микропатчами**: один запрос = одна цель = один коммит.

## Как дробить работу (рекомендуемый ритм)
1. Один запрос: только **1 подсистема** и максимум **3-5 файлов**.
2. Один запрос: не больше **~300-500 строк diff**.
3. В конце каждого запроса: `go test`/`tsc`/`verify` только по затронутому контуру.
4. Сразу фиксируй отдельным коммитом.

## Порядок шагов (с чего начать)
1. **R1.1: Подготовка каркаса editor-v3**
- Создать папки `frontend/features/editor-v3/{components,hooks,context,types,services}`.
- Ничего не менять в UX, только перенос типов/констант из `editor/page.tsx`.
- Результат: страница работает как раньше, но база для декомпозиции готова.

2. **R1.2: Вынести state в hook**
- Сделать `useEditorState` и перенести базовые состояния (selectedFile, dirty, saving, errors).
- UI не менять визуально.
- Результат: меньше логики в `page.tsx`, поведение без регрессий.

3. **R1.3: Ввести EditorContext**
- Обернуть страницу провайдером, подключить чтение состояния через context.
- Результат: убрать prop-drilling.

4. **R1.4: Вынести File actions**
- `useFileActions` (save, rename, move, delete, upload) + единые guard/disable флаги.
- Результат: централизованный контроль кнопок.

5. **R1.5: Вынести AI панель в компонент**
- Только перенос в `AIStudioPanel`, без новых фич.
- Результат: `page.tsx` заметно короче.

6. **R2.1: Action locks (anti-spam)**
- `useActionLocks` для `ai-suggest`, `ai-create-page`, `ai-generate-asset`.
- Результат: нельзя спамить кнопки в in-flight.

7. **R2.2: Единые статусы AI**
- Ввести state machine и баннер статуса (`подготовка/отправка/парсинг/готово/ошибка`).
- Результат: пользователю прозрачно, что происходит.

8. **R2.3: Локализация editor/AI**
- Вынести словарь RU и заменить разрозненные label’ы.
- Результат: консистентные кнопки и тексты.

9. **R3.1: Backend image endpoint (отдельно)**
- Только backend для генерации изображений + валидация mime/сигнатуры.
- Результат: API готов, frontend пока не трогаем.

10. **R3.2: Frontend image flow**
- Отдельная кнопка/форма генерации картинки + диагностика ошибок.
- Результат: рабочий прозрачный image flow.



PLEASE IMPLEMENT THIS MICRO-STEP: R1.1 (Editor V3 scaffold only)

Цель:
Подготовить каркас feature-модуля editor-v3 без изменения текущего поведения UI.

Ограничения:
1. Не менять бизнес-логику и UX.
2. Не менять backend.
3. Не рефакторить весь page.tsx целиком.
4. Максимум 5-7 файлов изменений.

Что сделать:
1. Создать структуру:
- frontend/features/editor-v3/components/
- frontend/features/editor-v3/hooks/
- frontend/features/editor-v3/context/
- frontend/features/editor-v3/services/
- frontend/features/editor-v3/types/

2. Вынести только базовые типы/константы из:
- frontend/app/domains/[id]/editor/page.tsx
в новые файлы:
- frontend/features/editor-v3/types/editor.ts
- frontend/features/editor-v3/types/ai.ts
- frontend/features/editor-v3/services/constants.ts

3. В page.tsx:
- заменить локальные дублирующиеся type/interface/const на импорты из новых файлов,
- НЕ трогать JSX-структуру и обработчики действий.

4. Добавить короткий README для модуля:
- frontend/features/editor-v3/README.md
с описанием назначения папок.

Критерии приемки:
1. Поведение editor страницы визуально и функционально не изменилось.
2. Локальные типы/константы в page.tsx уменьшены, импортируются из feature-модуля.
3. `npx tsc --noEmit` проходит.
4. `npm run -s verify:file-editor-route-v2` (или ближайший актуальный verify для editor-route) проходит.
5. Изменения компактные и понятные.

Формат результата:
1. Кратко перечислить измененные файлы.
2. Указать какие именно типы/константы вынесены.
3. Показать команды проверки и их итог.
4. Сделать 1 коммит:
`refactor(editor): bootstrap editor-v3 feature module scaffold`
