# name: CI

# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]

# jobs:
#   backend:
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: authserver
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-go@v5
#         with:
#           go-version: "1.22"
#       - name: Cache go modules
#         uses: actions/cache@v4
#         with:
#           path: |
#             ~/.cache/go-build
#             ~/go/pkg/mod
#           key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
#           restore-keys: |
#             ${{ runner.os }}-go-
#       - name: Go test
#         run: GOCACHE=$(pwd)/.cache/go-build go test ./...
#       - name: Go vet
#         run: go vet ./...

#   frontend:
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: frontend
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           cache: npm
#           cache-dependency-path: frontend/package-lock.json
#       - name: Install deps
#         run: npm ci
#       - name: Lint
#         run: npm run lint
#       - name: Build
#         run: npm run build

#   docker:
#     runs-on: ubuntu-latest
#     needs: [backend, frontend]
#     steps:
#       - uses: actions/checkout@v4
#       - name: Build auth image
#         run: docker build -t authserver:ci ./authserver
#       - name: Build migrate image
#         run: docker build -t authserver-migrate:ci ./authserver -f authserver/Dockerfile --target migrate || true
