global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "pbn-generator"
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:8080"]
        labels:
          service: backend
