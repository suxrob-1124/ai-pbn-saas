#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
CHECKER="$ROOT/scripts/check_no_secrets.sh"
if [[ -x "$CHECKER" ]]; then
  "$CHECKER" --staged
else
  echo "[pre-commit] secret checker not found at $CHECKER, skipping"
fi
